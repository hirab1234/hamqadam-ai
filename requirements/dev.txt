# --------------------------------------------------------------------------- #
# Development, testing and evaluation tooling.
#
# Deliberately does NOT `-r base.txt`, so it can be layered on top of either
# the pinned production stack or the relaxed local overlay:
#
#   pip install -r requirements/base.txt  -r requirements/dev.txt   # py3.12
#   pip install -r requirements/local.txt -r requirements/dev.txt   # py3.13
# --------------------------------------------------------------------------- #

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
pytest-benchmark==5.1.0
pytest-timeout==2.3.1
respx==0.22.0

ruff==0.8.4
mypy==1.14.0
types-PyYAML==6.0.12.20240917

scikit-learn==1.6.0
matplotlib==3.10.0
pandas==2.2.3
rich==13.9.4
